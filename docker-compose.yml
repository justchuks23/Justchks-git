version: "2.2"
services:
  db:
    image: mysql:latest
    container_name: data_base
    restart: 'always'
    volumes:
      - mydata:/var/lib/mysql
    environment: 
      MYSQL_ROOT_PASSWORD: root_password@mysql
      MYSQL_DATABASE: new-mysql
      MYSQL_PASSWORD: root_password@mysql
      MYSQL_HOST: db
    ports:
      - "8080:3306"    
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "db", "-u", "root", "-proot_password@mysql"] 
      timeout: 20s
      retries: 10
      interval: 10s
      
  # Redis
  redis-james-django-chukwu:
    image: redis:alpine
    container_name: redis_4_test
    ports:
      - "6379:6379"
    volumes:
      - mydata:/app/data

  # myApp
  cron-z2y-django:
    build: ./
    #image: $Z2Y_IMAGE
    container_name: cron-4-django
    volumes:
      - mydata:/app/data
      - static:/app/staticfiles
      - media:/app/media
    ports:
<<<<<<< HEAD
      - "8088:80"
=======
      - "8083:80"
>>>>>>> 8aafd084a30ced69062363dd47871b8ce1f326ae
    depends_on:
      db: 
        condition: service_healthy   
    #env_file:
      # - .env
    #environment:
      #- DJANGO_SUPERUSER_USERNAME=${DJANGO_SUPERUSER_USERNAME}
      #- DJANGO_SUPERUSER_EMAIL=${DJANGO_SUPERUSER_EMAIL
      #- DJANGO_SUPERUSER_PASSWORD=${DJANGO_SUPERUSER_PASSWORD}

  # Celery
  celery:
    restart: always
    build: ./
    container_name: celery_4_test
    command: celery -A backend worker -l INFO
    depends_on:
      - redis-james-django-chukwu
      - cron-z2y-django
    volumes:
      - mydata:/app/data

volumes:
  mydata:
  static:
  media:
